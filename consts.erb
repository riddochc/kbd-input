#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <linux/input.h>
#include <linux/uinput.h>
#include <sys/time.h>
#include <sys/types.h>
#include <unistd.h>

int main(int argc, char *argv[])
{
    printf("---\n");
    
% f = File.open('/usr/include/linux/uinput.h')
% f.each_line do |line|
%   if line =~ /^#define (UI_\w+)\s/
    printf("  %s: %u\n", "<%= $1 %>", <%= $1 %>);
%   end
% end

% f = File.open('/usr/include/linux/input.h')
% f.each_line do |line|
%   if line =~ /^#define (EVIOC\w+)\s/
    printf("  %s: %u\n", "<%= $1 %>", <%= $1 %>);
%   end
% end
}
